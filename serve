#!/bin/bash
# Start the local web UI at http://localhost:8765
cd "$(dirname "$0")"
export PATH="$HOME/.local/bin:$PATH"
echo ""
echo "  Reel Transcriber running at:  http://localhost:8765"
echo "  Press Ctrl+C to stop."
echo ""
exec uv run uvicorn app:app --host 127.0.0.1 --port 8765
